import { Box, Grid, Card, Paper, Typography, CardHeader, CardContent } from '@mui/material';

// ------------------------------

export default function Title(){
    <Typography variant='h1' component="h2">
        Les't choose options
    </Typography>
}